# AIsle

## Run

```bash
npm install
npm run dev
```

Open http://localhost:5173

Runs with no configuration. To use a live model instead of the built-in offline mode, copy `.env.example` to `.env` and set `AI_API_KEY` (any OpenAI-compatible endpoint).

## Build

```bash
npm run build
npm start
```
